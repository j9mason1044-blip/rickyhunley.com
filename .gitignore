.DS_Store
Thumbs.db
node_modules/

# Migration working notes, not part of the site
dns-snapshot.md

# Sanity Studio build output and local cache
studio/dist/
studio/.sanity/
studio/schema.json

# Written by build-static.js on every build, so committing it would churn a
# timestamp on each run. Netlify regenerates it during the deploy and publishes
# it from the working directory, which is the only copy that matters.
build-info.json
